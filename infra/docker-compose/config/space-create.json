{
    "identifier": "${KC_REALM}",
    "displayName": "${KC_REALM_DISPLAY_NAME}",
    "theme": "${KC_REALM_THEME}",
    "locales": ["en", "fr", "es"],
    "mfa": ["OTP"],
    "settings": {
        "login": {
            "registrationAllowed": true,
            "resetPasswordAllowed": true,
            "rememberMe": true,
            "registrationEmailAsUsername": true
        }
    },
    "adminUsers": [],
    "rootClient": {
        "clientId": "${KC_REALM}-${KC_IM_CLIENT_ID}",
        "clientSecret": "${KC_REALM_ROOT_CLIENT_SECRET}"
    },
    "smtp": {
        "from": "dev@komune.io",
        "fromDisplayName": "Dev Komune",
        "replyTo": "no-reply@komune.io",
        "replyToDisplayName": "No Reploy Komune",
        "host": "${SMTP_HOST}",
        "port": "${SMTP_PORT}",
        "ssl": "",
        "starttls": "",
        "auth": ""
    }
}
