window._env_ = {
  config: {
    im: {
      url: "${IM_URL_PUBLIC}",
    },
    fs: {
      url: "${FS_URL_PUBLIC}",
    },
    keycloak: {
      realm: "${KC_REALM}",
      clientId: "${KC_CONNECT_ADMIN_WEB_CLIENT_ID}",
      url: "${KC_URL_PUBLIC}"
    }
  }
};
