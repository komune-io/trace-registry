server:
  port: 8070

#spring:
#  webflux:
#    multipart:
#      maxInMemorySize: 20MB
#      maxHeadersSize: 20MB

cccev:
  url: http://localhost:8083

fs:
  url: http://localhost:8090

im:
  url: https://dev.connect.smart-b.io/im
  auth:
    url: https://auth.dev.connect.smart-b.io
    realm: sb-dev
    clientId: tr-smartb-ver-app
    clientSecret: 8d637328-3e2f-476c-9737-29bd2889df83

i2:
  issuers:
    -
      name: connect-sb-io
      authUrl: ${im.auth.url}
      realm: ${im.auth.realm}
      web:
        clientId: sb-plateform-web
      im:
        clientId: ${im.auth.clientId}
        clientSecret: ${im.auth.clientSecret}

ssm:
  signer:
    user:
      name: ssm-admin
      key: user/ssm-admin
    admin:
      name: ssm-admin
      key: user/ssm-admin
  couchdb:
    url: http://localhost:5984
    username: couchdb
    password: couchdb
    serviceName: s2-sample-did
  chaincode:
    url: http://localhost:9090

kb:
  url: http://localhost:9999

---
spring:
  config:
    activate:
      on-profile: init

server:
  port: 8000

---
spring:
  config:
    activate:
      on-profile: local

#i2:
#  issuers:
#    -
#      name: local
#      authUrl: http://localhost:8080/auth/
#      realm: alveole
#      web:
#        clientId: alveole-web
#      im:
#        clientId: alveole-api
#        clientSecret: 41a03e55-93c1-488d-9696-8df23e86a2a5
